"""
Tầng nghiệp vụ.

View chỉ nhận request, gọi service, rồi render. Mọi truy vấn và luật nghiệp vụ
nằm ở đây, nhờ vậy cùng một logic dùng được cho cả giao diện web, REST API và
script chạy nền mà không phải viết lại.
"""
